<!DOCTYPE html>
<html lang="pt-BR">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Transcript - {{channelName}}</title>
  <style>
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }
    
    html {
      background-color: #313338;
    }
    
    body {
      font-family: 'Segoe UI', 'Helvetica Neue', sans-serif;
      background-color: #313338;
      color: #dbdee1;
      line-height: 1.375;
      font-size: 15px;
    }
    
    .container {
      background-color: #313338;
      min-height: 100vh;
      gap: 8px;
      display: flex;
      flex-direction: column;
    }
    
    .header {
      background-color: #2c2f33;
      padding: 24px 20px;
      border-bottom: 1px solid #1e1f22;
      display: flex;
      align-items: center;
      gap: 16px;
    }
    
    .header h1 {
      color: #ffffff;
      font-size: 20px;
      font-weight: 700;
      margin: 0;
    }
    
    .header-info {
      display: flex;
      gap: 20px;
      margin-left: auto;
      font-size: 12px;
      color: #949ba4;
    }
    
    .messages-container {
      margin: 0 auto;
      max-width: 1500px;
      padding: 0 20px;
      flex: 1;
      display: flex;
      flex-direction: column;
    }
    
    .messages {
      display: flex;
      flex-direction: column;
    }
    
    .message {
      padding: 4px 8px 8px 8px;
      display: flex;
      gap: 12px;
      margin-top: 1px;
      border-radius: 4px;
      transition: background-color 0.2s ease;
    }
    
    .message:hover {
      background-color: rgba(79, 84, 92, 0.4);
    }
    
    .message-avatar {
      width: 40px;
      height: 40px;
      border-radius: 50%;
      object-fit: cover;
      margin-top: 4px;
      flex-shrink: 0;
    }
    
    .message-body {
      flex: 1;
      min-width: 0;
    }
    
    .message-header {
      display: flex;
      align-items: center;
      gap: 8px;
      margin-bottom: 2px;
      flex-wrap: wrap;
    }
    
    .message-author {
      font-weight: 600;
      color: #ffffff;
      font-size: 15px;
      cursor: default;
    }
    
    .message-timestamp {
      font-size: 12px;
      color: #949ba4;
      margin-left: 4px;
    }
    
    .message-content {
      color: #dbdee1;
      word-wrap: break-word;
      font-size: 15px;
      line-height: 1.375;
      white-space: pre-wrap;
      word-break: break-word;
      margin: 4px 0;
    }
    
    .embed {
      background-color: #2c2f33;
      border-left: 4px solid #5865f2;
      padding: 12px;
      border-radius: 4px;
      margin-top: 8px;
      max-width: 520px;
    }
    
    .embed-author {
      display: flex;
      align-items: center;
      gap: 8px;
      margin-bottom: 8px;
    }
    
    .embed-author-name {
      font-weight: 600;
      color: #ffffff;
      font-size: 14px;
    }
    
    .embed-title {
      font-weight: 600;
      color: #ffffff;
      margin-bottom: 4px;
      font-size: 15px;
    }
    
    .embed-description {
      color: #dbdee1;
      font-size: 14px;
      margin-bottom: 8px;
      line-height: 1.375;
    }
    
    .embed-fields {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(150px, 1fr));
      gap: 8px;
      margin-bottom: 8px;
    }
    
    .embed-field {
      min-width: 0;
    }
    
    .embed-field-inline {
      display: inline-block;
      min-width: 150px;
    }
    
    .embed-field-name {
      font-weight: 600;
      color: #ffffff;
      font-size: 12px;
      text-transform: uppercase;
      margin-bottom: 4px;
    }
    
    .embed-field-value {
      color: #dbdee1;
      font-size: 14px;
      line-height: 1.375;
    }
    
    .embed-image {
      max-width: 100%;
      border-radius: 4px;
      margin-top: 8px;
      max-height: 300px;
      object-fit: cover;
    }
    
    .embed-thumbnail {
      width: 80px;
      height: 80px;
      border-radius: 4px;
      object-fit: cover;
      float: right;
      margin-left: 12px;
      margin-top: 4px;
    }
    
    .embed-footer {
      color: #949ba4;
      font-size: 12px;
      margin-top: 8px;
      padding-top: 8px;
      border-top: 1px solid rgba(79, 84, 92, 0.3);
    }
    
    .attachment {
      background-color: #2c2f33;
      padding: 12px;
      border-radius: 4px;
      margin-top: 8px;
      border: 1px solid rgba(79, 84, 92, 0.3);
      max-width: 100%;
      width: fit-content;
    }
    
    .attachment-image {
      max-width: 100%;
      max-height: 400px;
      border-radius: 4px;
      margin-top: 8px;
      object-fit: cover;
    }
    
    .attachment-video {
      max-width: 100%;
      max-height: 400px;
      border-radius: 4px;
      margin-top: 8px;
    }
    
    .attachment-audio {
      width: 100%;
      margin-top: 8px;
      width: 300px;
    }
    
    .attachment-link {
      color: #00b0f4;
      text-decoration: none;
      font-size: 13px;
      word-break: break-all;
    }
    
    .attachment-link:hover {
      text-decoration: underline;
    }
    
    .components {
      display: flex;
      flex-wrap: wrap;
      gap: 8px;
      margin-top: 8px;
    }
    
    .button {
      padding: 8px 16px;
      border-radius: 4px;
      border: none;
      cursor: pointer;
      font-size: 14px;
      font-weight: 500;
      display: inline-flex;
      align-items: center;
      gap: 6px;
      transition: background-color 0.2s ease;
      user-select: none;
    }
    
    .button:hover:not(:disabled) {
      filter: brightness(1.2);
    }
    
    .button-1 {
      background-color: #5865f2;
      color: white;
    }
    
    .button-2 {
      background-color: #4e5058;
      color: white;
    }
    
    .button-3 {
      background-color: #57f287;
      color: #000;
    }
    
    .button-4 {
      background-color: #ed4245;
      color: white;
    }
    
    .button:disabled,
    .button-disabled {
      opacity: 0.5;
      cursor: not-allowed;
    }
    
    .select-menu {
      background-color: #1e1f22;
      color: #dbdee1;
      padding: 10px;
      border-radius: 4px;
      border: 1px solid rgba(79, 84, 92, 0.5);
      font-size: 14px;
      font-family: 'Segoe UI', 'Helvetica Neue', sans-serif;
      cursor: pointer;
      transition: border-color 0.2s ease;
    }
    
    .select-menu:hover:not(:disabled) {
      border-color: rgba(79, 84, 92, 0.8);
    }
    
    .select-menu:disabled {
      opacity: 0.5;
      cursor: not-allowed;
    }
    
    .select-menu option {
      background-color: #36393f;
      color: #dbdee1;
    }
    
    .text-input {
      background-color: #1e1f22;
      color: #dbdee1;
      padding: 10px 12px;
      border-radius: 4px;
      border: 1px solid rgba(79, 84, 92, 0.5);
      font-size: 15px;
      font-family: 'Segoe UI', 'Helvetica Neue', sans-serif;
      resize: vertical;
      min-height: 32px;
      transition: border-color 0.2s ease;
      width: 100%;
    }
    
    .text-input:hover:not(:disabled) {
      border-color: rgba(79, 84, 92, 0.8);
    }
    
    .text-input:focus {
      outline: none;
      border-color: #5865f2;
      box-shadow: 0 0 0 2px rgba(88, 101, 242, 0.1);
    }
    
    .text-input:disabled {
      opacity: 0.5;
      cursor: not-allowed;
      background-color: rgba(30, 31, 34, 0.5);
    }
    
    .text-input::placeholder {
      color: #72767d;
    }
    
    .text-input-label {
      font-size: 12px;
      font-weight: 600;
      color: #f2f3f5;
      margin-bottom: 8px;
      display: block;
      text-transform: uppercase;
    }
    
    .text-input-required {
      color: #ed4245;
    }
    
    .component-badge {
      display: inline-block;
      background-color: rgba(88, 101, 242, 0.2);
      color: #5865f2;
      font-size: 11px;
      padding: 2px 8px;
      border-radius: 12px;
      margin-left: 8px;
      font-weight: 600;
    }
    
    .footer {
      background-color: #2c2f33;
      padding: 16px 20px;
      text-align: center;
      font-size: 12px;
      color: #72767d;
      border-top: 1px solid #1e1f22;
      margin-top: auto;
    }
    
    .empty-state {
      text-align: center;
      padding: 60px 20px;
      color: #72767d;
      font-size: 16px;
    }
    
    @media (max-width: 768px) {
      .container {
        max-width: 100%;
      }
      
      .header {
        padding: 16px 12px;
        flex-direction: column;
        align-items: flex-start;
      }
      
      .header h1 {
        font-size: 18px;
      }
      
      .header-info {
        flex-direction: column;
        gap: 4px;
        margin-left: 0;
        width: 100%;
      }
      
      .messages-container {
        padding: 0 12px;
      }
      
      .message {
        gap: 8px;
      }
      
      .message-avatar {
        width: 32px;
        height: 32px;
      }
      
      .embed {
        max-width: 100%;
      }
      
      .attachment-image,
      .attachment-video {
        max-width: 100%;
      }
      
      .components {
        flex-direction: column;
      }
      
      .button {
        width: 100%;
        justify-content: center;
      }
      
      .select-menu {
        width: 100%;
      }
      
      .text-input {
        width: 100%;
      }
    }
  </style>
</head>
<body>
  <div class="container">
    <div class="header">
      <h1># {{channelName}}</h1>
      <div class="header-info">
        <div><strong>Servidor:</strong> {{guildName}}</div>
        <div><strong>Criado em:</strong> {{createdAtFull}}</div>
      </div>
    </div>
    
    <div class="messages-container">
      {{#messages}}
      <div class="messages">
        {{#.}}
        <div class="message">
          {{#avatarUrl}}<img src="{{avatarUrl}}" alt="{{author}}" class="message-avatar">{{/avatarUrl}}
          {{^avatarUrl}}<div class="message-avatar" style="background: #5865f2; display: flex; align-items: center; justify-content: center; color: white; font-weight: 600;">{{author.[0]}}</div>{{/avatarUrl}}
          
          <div class="message-body">
            <div class="message-header">
              <span class="message-author">{{author}}</span>
              <span class="message-timestamp">{{timestamp}}</span>
            </div>
            
            {{#content}}<div class="message-content">{{content}}</div>{{/content}}
            
            {{#hasEmbeds}}
            {{#embeds}}
            <div class="embed" style="{{#color}}border-left-color: {{color}};{{/color}}">
              {{#author}}<div class="embed-author"><span class="embed-author-name">{{author}}</span></div>{{/author}}
              {{#title}}<div class="embed-title">{{title}}</div>{{/title}}
              {{#description}}<div class="embed-description">{{description}}</div>{{/description}}
              
              {{#fields}}
              {{#fields}}
              <div class="embed-field{{#inline}} embed-field-inline{{/inline}}">
                <div class="embed-field-name">{{name}}</div>
                <div class="embed-field-value">{{value}}</div>
              </div>
              {{/fields}}
              {{/fields}}
              
              {{#thumbnail}}<img src="{{thumbnail}}" alt="thumbnail" class="embed-thumbnail">{{/thumbnail}}
              {{#image}}<img src="{{image}}" alt="embed image" class="embed-image">{{/image}}
              {{#footer}}<div class="embed-footer">{{footer}}</div>{{/footer}}
            </div>
            {{/embeds}}
            {{/hasEmbeds}}
            
            {{#hasAttachments}}
            {{#attachments}}
            <div class="attachment">
              {{#isImage}}<img src="{{url}}" alt="{{name}}" class="attachment-image">{{/isImage}}
              {{#isVideo}}<video src="{{url}}" controls class="attachment-video"></video>{{/isVideo}}
              {{#isAudio}}<audio src="{{url}}" controls class="attachment-audio"></audio>{{/isAudio}}
              <div><a href="{{url}}" class="attachment-link" target="_blank">📎 {{name}}</a></div>
            </div>
            {{/attachments}}
            {{/hasAttachments}}
            
            {{#hasComponents}}
            <div class="components">
              {{#components}}
              {{#buttons}}
              <button class="button button-{{style}}{{#disabled}} button-disabled{{/disabled}}" {{#disabled}}disabled{{/disabled}}>{{#emoji}}{{emoji}} {{/emoji}}{{label}}</button>
              {{/buttons}}
              {{#selectMenus}}
              <div style="width: 100%;">
                <select class="select-menu" disabled>
                  <option>{{placeholder}}</option>
                  {{#options}}
                  <option>{{label}}</option>
                  {{/options}}
                </select>
                <span class="component-badge">{{type}} Select</span>
              </div>
              {{/selectMenus}}
              {{#textInputs}}
              <div style="width: 100%; margin-top: 8px;">
                <label class="text-input-label">{{label}}{{#required}}<span class="text-input-required"> *</span>{{/required}}</label>
                <textarea class="text-input" disabled rows="{{#style}}{{style}}{{/style}}{{^style}}1{{/style}}" placeholder="{{placeholder}}"></textarea>
                <span class="component-badge">{{style}} Text</span>
              </div>
              {{/textInputs}}
              {{/components}}
            </div>
            {{/hasComponents}}
          </div>
        </div>
        {{/.}}
      </div>
      {{/messages}}
      
      {{^messages}}
      <div class="empty-state">
        <p>Nenhuma mensagem para exibir</p>
      </div>
      {{/messages}}
    </div>
    
    <div class="footer">
      <p>Fechado em: {{closedAtFull}}</p>
      <p style="margin-top: 4px; opacity: 0.7;">Transcript gerado automaticamente</p>
    </div>
  </div>
</body>
</html>